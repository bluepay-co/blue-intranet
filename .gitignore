# Dependências do Node
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# Arquivos de Build/Compilação do TypeScript
dist/
build/
*.tsbuildinfo

# Output do tsc em-lugar (back-end sem outDir configurado)
back-end/**/*.js
back-end/**/*.js.map
back-end/**/*.d.ts
back-end/**/*.d.ts.map

# Diretórios de runtime que não vão para o repo
back-end/uploads/
Fullstack-dashboard/

# Documentos de planejamento local
plano-metricas-gerais.md
"Implementação de Métricas.md"
Implementação\ de\ Métricas.md

# Variáveis de Ambiente e Arquivos de Configuração Sensíveis (Segurança!)
.env
.env.local
.env.development.local
.env.test.local
.env.production.local
*.env

# Arquivos do Sistema Operacional (Ubuntu/Mac/Windows)
.DS_Store
Thumbs.db
.directory

# Ferramentas de IA / Claude Code
.claude/
test-results/
CLAUDE.md
# Querys de apoio para execução manual no banco (uso local apenas)
querys/

# Configurações de editores de código (Opcional, mas bom ter)
.vscode/
.idea/
*.suo
*.ntvs*
*.njsproj
*.sln
*.swp


# Certificados TLS (gerados na VM, nunca versionar)
certs/

# Dump de banco (dados reais — nunca versionar)
*.dump

# Planilhas de metas (dados de negócio, uso local)
Metas Bluepay*.csv
